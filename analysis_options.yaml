include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    deprecated_member_use: ignore
    use_build_context_synchronously: ignore
    avoid_print: ignore
    unused_field: ignore

linter:
  rules:
    # Reglas extra opcionales; el set base ya viene de flutter_lints.
